# GitHub comments to post (copy/paste)

## rusty-kaspa#615 — support PR #906

Integrator rehearsal from kaspa-frontier-engine: exchange deposit tooling needs enriched input UTXOs on owned nodes, not only public REST.

GetVirtualChainFromBlockV2 covers part of this; GetBlocksV2 (PR #906) matches our fee + source-address use cases for block-scoped ingestion.

Evidence: TN10 2/2 owned-node gate + wRPC deposit journal (Sep 14 2026).
Repo: https://github.com/explife365/kaspa-frontier-engine
CLI fee probe: `cargo run --release --bin tn10-return-address -- <txid> --fee`

Happy to run integration tests against a node build that includes #906 when available.

---

## rusty-kaspa#435 — integrator shim + upstream RPC ask

Until kaspad exposes return-address on UTXO creation, we shipped a TN10 REST chain-walk shim:

`cargo run --release --bin tn10-return-address -- <deposit_txid> [vout]`

Spec match: first input only → `Option<Address>`. Fails closed when parent tx is pruned (documents need for node-side UTXO diff lookup per issue description).

Willing to help review/test an upstream RPC implementation; our crate can swap to native when shipped (see `scripts/integrator_shims.py`).

Repo: https://github.com/explife365/kaspa-frontier-engine
