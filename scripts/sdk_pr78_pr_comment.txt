Follow-up from TN10 integrator rehearsal (public repo: https://github.com/explife365/kaspa-frontier-engine).

PR #78 is **merged** — thank you. Pinned `kaspa` 2.0.2rc1 on PyPI still drops `computeBudget` on `TransactionInput.to_dict()`, so native Toccata v1 SilverScript broadcasts remain fail-closed until a **published wheel** includes the fix.

Repro gate:

```bash
python scripts/tn10_sdk_gate.py --json
```

Current output: `readyNative: false`, `computeBudgetOkNative: false`, `readyWithDevPatch: true`, `pr78.state: MERGED`.

**Shipped on TN10 since merge (offline tn10-proof OK):**
- HTLC claim + refund fixtures
- Escrow 2-of-3 genesis → release_seller
- L1↔L2 HtlcBridgeRelease on Galleon (wiKAS claim live)

Happy to refresh public proof fixtures and link txids after the PyPI RC lands. Not a kaspad change.
