Cross-link: public TN10 integrator rehearsal with a fail-closed SDK gate for this failure mode:

https://github.com/explife365/kaspa-frontier-engine

`TransactionInput.compute_budget` is set in memory but dropped on `to_dict()` in pinned `kaspa` 2.0.2rc1, so RPC round-trip zeros the budget → "script units exceeded" on TN10.

Repro:

```bash
python scripts/tn10_sdk_gate.py --json
```

Fix tracked in [kaspa-python-sdk#78](https://github.com/kaspanet/kaspa-python-sdk/pull/78) (`computeBudget` in `TryToPyDict`). Not rusty-kaspa#1074 (WASM generator).

**Update:** all three SilverScript reference apps now have public TN10 proof fixtures (counter, timelock vault, restricted swap). Vault release: `33743ee5fc224aae3e01ced94f29c6ca3238d7dc4a3c92b8a2c49948cc575d4f`. Swap: `9dbb658daab4dab916f047871c74508b653d2e4c37aeb6e34ab1a2a9b049a8ae`. Native wheel path still fail-closed until #78 publishes.
