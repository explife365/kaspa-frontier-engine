Post to: Kaspa Discord #smart-contract-brainstorming (primary). Alt: #developers.
Not: CertiK/Skynet (Foundation ops — certik_foundation_submission.md).

Toccata integrator path on TN10 — reference proofs + L1↔L2 bridge live

Toccata v1 is live on kaspad (mass, compute_budget, covenant_id). TN10 has heavy covenant activity. kaspa-python-sdk#78 is **merged**; native broadcasts stay fail-closed until a **published wheel** ships computeBudget on TransactionInput.to_dict().

**Public proof bundles (testnet-10, offline tn10-proof OK):**
- Counter: genesis → add → subtract
- Timelock vault: genesis → release
- Restricted swap: genesis → swap
- HTLC claim: genesis → claim (payment_hash 0x48544C43)
- HTLC SHA256 claim: genesis → claim (preimage b"HTLC"; claim e1d5eecf…)
- HTLC refund: genesis → refund
- Escrow 2-of-3: genesis → release_seller (seller + arbiter, 1 tKAS lock cap)

**L1↔L2 HTLC bridge (Galleon testnet):**
- Int-tag demo: HtlcBridgeRelease deployed; wiKAS vault funded + claimed (0xa41219…)
- SHA256 path: L1 claim live; L2 HtlcBridgeReleaseSha256 deployed + vault funded (0xdf22ee…); claim gas pending
- Playbook: examples/l1_l2_htlc_bridge.py --json

Repro:
  python scripts/tn10_sdk_gate.py --json
  cargo run --release --bin tn10-proof -- fixtures/tn10-escrow-2of3-proof.json --offline
  python examples/l1_l2_bridge_release.py --status

Public integrator repo:
  https://github.com/explife365/kaspa-frontier-engine

**How to help today:**
- Watch kaspa-python-sdk releases for post-#78 wheel; re-run tn10_sdk_gate.py --json (readyNative should flip true)
- Review wallet/explorer covenant decode UX (kascov indexes TN10 covenants)
- Node operators: synced TN10 kaspad with --utxoindex (scripts/tn10_node_onboard.ps1)

Until the wheel publishes, TN10 dev rehearsal uses TN10_SDK_DEV_PATCH=1 (see README). Not a kaspad consensus issue.

Evidence pack: powershell -File scripts/integrator_evidence_pack.ps1 (139 Rust + 95 pytest)
