TN10 integrator rehearsal — public repo for Core, SDK, and exchange teams

https://github.com/explife365/kaspa-frontier-engine

Toccata is live on testnet-10. This crate rehearses owned-node health, DAA-confirmed deposits/withdrawals, wRPC ingestion, and SilverScript covenant proof bundles.

**Shipped on TN10 (offline tn10-proof OK):**
- Counter, timelock vault, restricted swap
- HTLC claim + refund
- Escrow 2-of-3 genesis → release_seller (1 tKAS lock cap; change returned to funder)

**L1↔L2 (Galleon testnet):**
- HtlcBridgeRelease deployed; wiKAS vault funded + claimed with L1-matching preimage demo
- Playbook: `python examples/l1_l2_htlc_bridge.py --json`

Repro:
```
python scripts/tn10_sdk_gate.py --json
cargo run --release --bin tn10-proof -- fixtures/tn10-escrow-2of3-proof.json --offline
python examples/l1_l2_bridge_release.py --status
powershell -File scripts/integrator_evidence_pack.ps1
```

**Native broadcast:** kaspa-python-sdk#78 merged — awaiting PyPI wheel (`readyNative` still false on 2.0.2rc1). TN10 dev path: `TN10_SDK_DEV_PATCH=1`.

**Node adoption:** `scripts/tn10_node_onboard.ps1` + `tn10_adoption_scorecard.py` (CEX/N-of-M gate).

CEX checklist: scripts/cex_production_runbook.md
getUtxosByCovenantId KIP: scripts/getUtxosByCovenantId_kip_outline.md

Not in scope: consensus patches, DAGKnight activation, or L1 EVM/stables.
